letters = input().split(", ")
dictionary = {}
for letter in letters:
    dictionary[letter] = ord(letter)





# characters = input().split(", ")
# dictionary = {key: ord(key) for key in characters}

print(dictionary)